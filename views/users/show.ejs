<% include ../partials/header %>

<div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-4">
        <div class="row">
            <div class="col-md-7">
                <h2><%= user.firstName + " " + user.lastName %></h2>
            </div>
            <div class="col-md-5">
                <% if(currentUser && user._id.equals(currentUser._id) || currentUser && currentUser.isAdmin) { %>
                    <a class="btn btn-warning" href="/users/<%= user._id %>/edit">Edit</a>
                    <form id="delete-form" action="/users/<%= user._id %>?_method=DELETE" method="POST">
                        <button class="btn  btn-danger">Delete</button>
                    </form>
                 <% } %>%>
            </div>
        </div>
        
        <div class="list-group">
            <li class="list-group-item"><img class="img-thumbnail" src="<%= user.avatar %>" alt="user profile pic"></li>
            <li class="list-group-item">Email: <a href="mailto:<%= user.email %>"><%= user.email %></a></li>
            <li class="list-group-item">Lorem ipsum, dolor sit amet consectetur adipisicing elit. Facere voluptas modi quas fugit aspernatur velit ad, earum obcaecati blanditiis exercitationem, magnam corrupti, tenetur expedita repudiandae soluta nisi laboriosam magni iste.

                Lorem ipsum dolor sit amet, consectetur adipisicing elit. Temporibus autem, in repudiandae quis doloribus perferendis incidunt. Repudiandae, provident? Fuga molestiae odit quibusdam adipisci dignissimos impedit, vero ipsa autem natus itaque.</li>
        </div>
    </div>
    <div class="col-md-1"></div>
    <div class="col-md-6">
        <h3><u><%= user.username + "'s campgrounds list:" %></u></h3>
     <ul>
       <% campgrounds.forEach(function(campground) { %>
        <li><a href="/campgrounds/<%= campground.id %>"><%= campground.name %></a></li>

       <% }); %>
     </ul>
    </div>
</div>

<% include ../partials/footer %>